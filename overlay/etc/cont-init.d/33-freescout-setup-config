#!/usr/bin/with-contenv bash

set -euo pipefail
export BF_E=`basename ${0}`


#======================================================================================================================
# If FREESCOUT_STORAGE_SRC doesn't exist, the content setup has not worked correctly.
#======================================================================================================================

[[ ! -d ${FREESCOUT_STORAGE_SRC} ]] && bf-error "${FREESCOUT_STORAGE_SRC} does not exist."


#======================================================================================================================
# Run setup config executable.
#======================================================================================================================

${FREESCOUT_LIB}/setup-config
